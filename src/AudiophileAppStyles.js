import { makeStyles } from "@material-ui/core";

const AppStyles = makeStyles({
  header: {
    width: "100%",
    position: "absolute",
    top: "0",
    zIndex: "1000",
  },
  main__content: {
    // width: "100%",
    // position: "absolute",
    // top: "0",
  },
  footer: {},
});

export default AppStyles;
