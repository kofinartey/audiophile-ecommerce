import { makeStyles } from "@material-ui/core";

const AppStyles = makeStyles({
  header: {
    width: "100%",
    position: "absolute",
    top: "0",
    zIndex: "2000",
  },
});

export default AppStyles;
